package com.hibernate.DB;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

import com.hibernate.dto.AveragedMovieRating;
import com.hibernate.dto.FNVhash;

public class DbAverageMovieRating {
public static void main(String[] args){
		/*
		AveragedMovieRating amr =new AveragedMovieRating();
		
		amr.setMovieid(FNVhash.hash64("utpal@gmail.com"));
		amr.setUsertype(0);
		amr.setWeightedScore(0.0);
		
				
		SessionFactory sessionFactory = new Configuration().configure().buildSessionFactory();
		Session session = sessionFactory.openSession();
		session.beginTransaction();
		session.save(amr);
		// session.flush();
         ///session.clear();
		session.getTransaction().commit();
		*/
}
}
